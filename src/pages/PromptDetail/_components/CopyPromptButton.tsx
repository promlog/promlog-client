import Button from '../../../components/Button/Button';

const CopyPromptButton = () => {
  return (
    <Button icon="copy" className="w-full px-6 py-4 rounded-xl">
      프롬프트 복사하기
    </Button>
  );
};

export default CopyPromptButton;
