import { Icon } from '../../Icon/Icon';

const WithdrawIcon = () => {
  return (
    <div className="w-16 h-16 bg-red-100 rounded-full flex items-center justify-center">
      <Icon name="triangleAlert" size="xl" fill="none" stroke="#e7000b" />
    </div>
  );
};

export default WithdrawIcon;
